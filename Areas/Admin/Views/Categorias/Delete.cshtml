@model Categoria

@{
    ViewData["Title"] = "Excluir Categoria";
}

<h1 class="mb-4"><i class="fa-solid fa-trash text-coelho-vermelho"></i> Excluir Categoria</h1>

<div class="card admin-card">
    <div class="card-body">
        <h5 class="mb-3">Tem certeza que deseja excluir esta categoria?</h5>
        <dl class="row">
            <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Nome)</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.Nome)</dd>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-coelho-outline-danger"><i class="fa-solid fa-trash"></i> Excluir</button>
            <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
        </form>
    </div>
</div>
